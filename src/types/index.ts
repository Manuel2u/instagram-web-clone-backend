type signUpInfo = {
    username : string,
    email : string,
    fullName : string,
    password : string
}

type signInInfo = {
    email_username : string,
    password : string,
}

export {signUpInfo, signInInfo}